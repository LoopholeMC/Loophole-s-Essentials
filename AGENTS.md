# Project Notes

## Current Structure

- `Loophole-Essentials` is the main project to work on in this directory.
- `ClickCrystals-1.4.0` is the base mod that `Loophole-Essentials` targets as an addon.
- `No-oNe-Addon-latest` is an existing addon that should be used as a template/reference when building addon features or structure.
- `Mods-Turned-Into-Modules` is not part of active work right now and should be ignored until those source mods update to Minecraft `26.1.2`.

## Current State

- The addon category and category mixin have been restored for `Loophole-Essentials`.
- Successfully archived `Anchor-Macro` and its purely related dependency `MixinMultiPlayerGameMode` to the `Back Ups` folder to prepare for a complete remake. All related `AGENTS.md` history has been moved to the archived folder.
- Successfully archived `AimAssist` and its purely related dependencies to the `Back Ups` folder to prepare for a complete remake. All related `AGENTS.md` history has been moved to the archived folder.
- `Loophole-Essentials` now registers two addon modules, `Auto-Lunge` and `Trigger-Bot`, under `Loophole's Essentials`.
- `Loophole-Essentials` builds successfully after archiving `Anchor-Macro` and `AimAssist`, with `Auto-Lunge` and `Trigger-Bot` currently registered as the active addon modules.
- `Loophole-Essentials` now mirrors the `No-oNe-Addon-latest` persistence approach by delaying ClickCrystals config/profile loading until after addon modules are registered, so addon module enabled states and settings load through the normal shared config/profile system instead of per-module delayed loads.
- The `Loophole's Essentials` category injection now explicitly preserves `No-oNe-Addon-latest`'s real `Addon` category object and, when that addon is present, forces the order `Addon -> Loophole's Essentials -> Custom Made` so the template category no longer overwrites or displaces the `Loophole's Essentials` section.
- `ClickCrystals-main` has been removed from the workspace, and `ClickCrystals-1.4.0` is now the active base-mod reference going forward.
- `Loophole-Essentials` now includes an addon-specific dual-value custom slider setting for range-based values such as randomized delays, with independent lower/upper handles, support for equal values including `0`, normal config persistence through a single encoded setting entry, and helper methods for retrieving a randomized value within the saved range.
- Reminder: whenever a setting depends on another setting's state in this workspace, implement it as a child setting that hides when its parent condition is inactive, and keep the module section ordering synced accordingly.
- `LoopholeEssentialsAddon` now skips its own delayed config/profile load when `No-oNe-Addon-latest`'s `AddonManager` is present, avoiding duplicate shared config initialization while still keeping standalone addon loading intact.
- `RangeDoubleSetting` values now persist across game restarts again in all addon modules: `ModuleFile.revert()` is patched through a new addon mixin so saved string-backed range values reload correctly instead of falling back to defaults, and `Loophole-Essentials` builds successfully with the persistence fix applied.
- Child-hidden addon settings now persist correctly as well: addon modules save hidden settings through an addon provider hook, and `Config.loadModule()` reapplies them even when they are currently hidden from the module editor, so toggling child visibility no longer drops saved values across saves or restarts.
- `Loophole-Essentials` now resets addon runtime state on `GameLeaveEvent` instead of only on manual module disable: `Trigger-Bot` now clears its live session state on leave/rejoin.
- `Trigger-Bot` has been rebuilt from scratch with render-validated target stability plus tick-start attack commits so it only queues one vanilla left-click per cooldown cycle and reduces flick-away air swings compared with naive frame-spam behavior.
- `Trigger-Bot` currently attacks only while the crosshair is on a valid player target, the client is not using an item, the main hand matches the configurable `allowed-held-items` list, and the randomized `attack-progress-threshold` range is satisfied.
- `Trigger-Bot` supports three combat branches from the requested spec: grounded swings, airborne falling swings, and sprint-gated uppercut swings inside a configurable `uppercut-distance-window`, while treating water, lava, and cobweb contact as grounded for those movement checks.
- `Trigger-Bot` now includes a configurable sprint reset section with `sprint-reset-mode` (`Off`, `S-Tap`, `W-Tap`), a `tap-distance-window`, and randomized `tap-duration` now capped at `1.000`: `S-Tap` now suppresses held `W` and briefly presses `S` to overpower forward movement before restoring normal input, while `W-Tap` only releases `W` for the configured duration without forcing `S` off; taps are distance-gated when they start, then continue for their configured duration unless jump, grounded-state loss, falling-state change, screen interruption, or other real runtime abort conditions stop them.
- `Trigger-Bot` no longer cancels an already active `S-Tap` or `W-Tap` just because the crosshair leaves the enemy after the tap has started.
- `Trigger-Bot` restores forward/back input from tracked real keypress state instead of leaving movement keys stuck, and a new addon `AccessorKeyMapping` mixin queues the attack through Minecraft's normal pending click counter rather than the legacy ClickCrystals attack helper.
- `Loophole-Essentials` builds successfully with the rebuilt `Trigger-Bot`, its new keymapping accessor mixin, and the updated module registration.
- `Trigger-Bot` sprint reset taps now honor the randomized `tap-duration` range instead of collapsing to a single tick: tap continuation no longer reuses sprint-required grounded attack validation, so `S-Tap` and `W-Tap` can keep running for their configured duration while still cancelling on the intended safety conditions.
- `Trigger-Bot` tap cancellation now restores held movement keys from the live physical key state rather than the module-forced keybinding state, so releasing a sprint reset while still holding `W` or `S` no longer requires letting go and pressing the key again.
- `Trigger-Bot` falling attacks now wait for a real crit-ready airborne state instead of firing on the first downward frame: the falling branch now requires actual downward motion, positive `fallDistance`, and cleared sprint state, and it suppresses uppercut/falling attacks during the brief descending transition before those crit conditions are met.
- `Trigger-Bot` falling-hit branching now ignores the `require-sprinting` gate, so falling attacks can fire whether or not the player is sprinting, while grounded and ascending/uppercut attacks still follow the sprint requirement setting.
- `Trigger-Bot` sprint reset taps now only start and continue while the player is truly on the ground via `onGround()`: air, cobwebs, water, and lava no longer count as valid tap-activation surfaces even though some of those states still count as grounded-like for other combat branching.
- `Auto-Lunge` is now implemented as a left-click hotbar swap module that always searches for a fixed `spear[lunge]` in the hotbar, uses a configurable `blocked-held-items` text box defaulting to `spear[lunge]` to suppress activation while holding matching items, keeps the randomized dual-value `switch-back-delay` range from `0.000` to `0.100`, follows Trigger-Bot-matching valid-player target suppression plus Elytra gliding and full-cooldown safety checks, defers switch-back while use input is active, and cancels the automatic switch-back whenever the player manually changes hotbar slots before restoration.
- `Auto-Lunge` now exposes a parent `switch-back` toggle plus child switch-back controls: `switch-back-delay`, `wait-for-no-use-before-switch-back`, and `cancel-switch-back-on-manual-slot-change` only show while switch-back is enabled, all default to the previous behavior, hidden child values persist through config saves, and disabling either child now lets players opt out of right-click deferral or manual-slot-change cancellation without removing delayed switch-back entirely.
- `Loophole-Essentials` now registers eight active addon modules under `Loophole's Essentials`: `Auto-Lunge`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Fast EXP`, `Jump-Reset`, `Key-Pearl`, and `Trigger-Bot`.
- `Fast EXP` is now implemented as a hard-coded experience-bottle helper that only runs while the use key is held with an `experience_bottle` in the main hand, uses the addon dual-value `use-delay` slider from `0.000` to `0.100` with a default of `0.000-0.050`, cancels its pending timer as soon as the hold/item/play conditions break, and queues each extra use through Minecraft's normal keybind click path instead of ClickCrystals' legacy scripted right-click input.
- `Fast EXP` no longer polls its randomized delay only on tick start: it now schedules one delayed use at a time, repicks a fresh `use-delay` value after every queued bottle click, preserves sub-tick randomness from the dual-value slider more accurately, and immediately invalidates any pending delayed use when right-click, held item, or active-play conditions break.
- `Loophole-Essentials` now includes a reusable addon-specific activation input setting for future modules through `LoopholeListenerModule#createMouseButtonSetting()`: it supports keyboard keys plus `Right Click`, `Middle Click`, `Mouse 4`, `Mouse 5`, and `NONE` in the same saved integer setting, normalizes older saved unsupported mouse-button values away on reload, and the addon builds successfully with the corrected input support.
- The addon activation-input selector now behaves like a proper bind box in the UI: clicking it arms an internal listening state instead of relying on the current hovered/selected child alone, keyboard keys now bind correctly for module use, `RMB`/`MMB`/`Mouse 4`/`Mouse 5` can be assigned even if the cursor drifts onto other GUI elements while listening, and `LMB` binding support has been removed entirely.
- `Auto-Web` is now implemented as a cobweb-only hotbar helper driven by the addon activation-input setting: while the selected key or supported mouse button is held and Minecraft is showing a real non-air block outline target, it swaps to a hotbar `cobweb`, keyboard bindings now start sessions correctly, guarantees an immediate first placement for non-`Right Click` binds, rerolls a randomized dual-value `use-delay` now capped at `0.500` before every later queued right-click, cancels pending delayed placements immediately when the hold or valid non-air block-target conditions break, and optionally switches back to the original slot on release with a child option to stay on the user's manually changed slot instead.
- `Key-Pearl` now behaves as a one-shot-per-press hotbar pearl helper driven by the shared addon bind setting: when the selected key or supported mouse button is pressed while playing, a hotbar `ender_pearl` exists, and pearls are not on cooldown, it swaps to that pearl slot, waits a randomized dual-value `use-delay` from `0.000` to `0.100`, queues one pearl throw through Minecraft's normal use-key click path, then optionally waits a randomized `switch-back-delay` from `0.000` to `0.100` and restores the original slot without needing the bind to be released, with a child option to stay on the user's manually changed slot instead; holding the bind no longer retriggers repeated throws before release, and `Loophole-Essentials` builds successfully with the corrected cooldown-aware switch-back behavior.
- `Block-Spam` now has a real module description and now defaults its held-item matcher to `plank,log,cobblestone`: it remains a held activation-input placement helper driven by the shared addon bind setting, only runs while the main-hand item matches that configurable comma-separated text field and Minecraft is showing a real non-air block outline target, queues normal client right-clicks through Minecraft's keybind click path, guarantees an immediate first placement for non-`Right Click` binds, rerolls a randomized dual-value `use-delay` from `0.000` to `0.500` before every later queued click, and invalidates any pending delayed placement immediately when the bind, held-item match, or valid highlighted block-target condition breaks.
- `Jump-Reset` is now implemented as a passive damage-reactive helper: on real self `EntityDamageEvent` hits from a valid player attacker, it tracks first-hit, second-hit, and third-plus hit streak tiers with a short attacker-aware reset window, skips activation while the player is on fire, requires `onGround()`, uses one shared addon dual-value `player-distance-window` slider as a fixed minimum/maximum allowed enemy distance for all tiers, rolls separate configurable `first-hit-chance`, `second-hit-chance`, and `third-hit-plus-chance` percentages, and now triggers jump through ClickCrystals' legacy held-jump input pulse (`InteractionUtils.inputJump()`) instead of the key-click path when a qualifying tier roll passes; `Loophole-Essentials` builds successfully with the corrected jump input behavior.
- `Auto-Sprint` is now implemented as a lightweight pre-tick sprint helper: while the player is actively holding the vanilla forward key and is not already sprinting, it triggers ClickCrystals' legacy sprint toggle input (`InteractionUtils.inputToggleSprint()`) once per forward-hold attempt instead of spamming repeated presses every tick, resets its internal latch when forward is released or sprinting starts, and `Loophole-Essentials` builds successfully with the new module registered.
- `Auto-Drain` is now implemented as a passive pre-tick empty-bucket helper: when the crosshair is on a real pickupable `Blocks.WATER` source, at least one valid player is within the configurable `player-range` and `player-fov`, and a hotbar `bucket` exists, it swaps to that empty bucket, waits the randomized dual-value `use-delay` from `0.010` to `0.035` by default, queues one normal client right-click through the keybind click counter, then optionally waits a randomized dual-value `switch-back-delay` from `0.010` to `0.035` plus a one-tick post-use safety buffer before restoring the original slot; it excludes obvious flowing/fake-source targets, ignores water positions the player placed during the current enabled session, uses a conservative adjacent-source heuristic to avoid likely infinite-source drains, and supports a parent `switch-back` toggle with a child `cancel-switch-back-on-manual-slot-change` option whose hidden state persists through config saves.
- `Loophole-Essentials` now registers nine active addon modules under `Loophole's Essentials`: `Auto-Drain`, `Auto-Lunge`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Fast EXP`, `Jump-Reset`, `Key-Pearl`, and `Trigger-Bot`, and the addon builds successfully with the new `Auto-Drain` module included.
- `Auto-Drain` now detects aimed water with its own fluid-aware look scan instead of relying only on the raw `mc.hitResult` block target, so opponent-placed bucket water that is visibly under the crosshair now triggers the drain sequence more reliably while still keeping the existing placed-water exclusion, infinite-source guard, and randomized switch-back flow; `Loophole-Essentials` builds successfully with the trigger fix applied.
- `Auto-Clutch` is now implemented as a passive pre-tick water-bucket clutch helper: while the player is falling at `vel_y <= -0.7`, airborne, holding a `water_bucket`, and aiming at the top face of a real block with no water already under the crosshair, it queues one normal Minecraft use-key click to place the clutch water, waits a configurable addon dual-value `pickup-delay` defaulting to `0.045-0.050`, then only reclaims that exact placed source through `KeyMapping.click(((AccessorKeyMapping) mc.options.keyUse).loopholeEssentials$getBoundKey())` after the player has actually touched the clutch water, the fall damage has been neutralized, and the source is still visibly under the crosshair; `Loophole-Essentials` now registers ten active addon modules under `Loophole's Essentials`: `Auto-Clutch`, `Auto-Drain`, `Auto-Lunge`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Fast EXP`, `Jump-Reset`, `Key-Pearl`, and `Trigger-Bot`, and the addon builds successfully with the new clutch module included.
- `Auto-Pot` is now implemented as a one-shot splash-potion helper driven by the shared addon activation-input setting: on each press, while the client is playing and attack input is not active, it checks a shared comma-separated `allowed-potions` text field defaulting to `instant_health`, matches splash potions by both potion type and contained effects so `instant_health` catches all healing levels, treats offhand as a valid available potion source alongside the hotbar, swaps to a matching hotbar splash potion only when neither hand already holds one, waits a randomized addon dual-value `use-delay` from `0.000` to `0.100` defaulting to `0.000-0.010`, then queues the throw through Minecraft's normal use-key click path; `Loophole-Essentials` now registers eleven active addon modules under `Loophole's Essentials`: `Auto-Clutch`, `Auto-Drain`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Fast EXP`, `Jump-Reset`, `Key-Pearl`, and `Trigger-Bot`, and the addon builds successfully with the new `Auto-Pot` module included.
- `Auto-Pot`'s `allowed-potions` matcher now normalizes vanilla potion variants and common names correctly: typed entries such as `strength`, `healing`, `harming`, `swiftness`, `leaping`, `instant_health`, and `instant_damage` now match the intended splash potion regardless of vanilla `long_` or `strong_` prefixes, so the text field works by base potion family instead of depending on exact raw variant IDs; `Loophole-Essentials` builds successfully with the matcher fix applied.
- `Loophole-Essentials` now includes a shared addon `PotionMatcher` helper so splash-potion text filters stay consistent anywhere the addon needs configurable potion matching instead of duplicating `Auto-Pot`'s normalization logic.
- `Pot-Refill` is now implemented as an inventory-open splash-potion refill helper: while the player inventory or creative inventory screen is open, if the cursor is hovering a matching splash potion from the configurable comma-separated `allowed-potions` list, it waits a randomized addon dual-value `refill-delay` from `0.000` to `0.050`, then performs the same vanilla-style hovered-slot swap as pressing hotbar keys `1`-`9` or offhand swap to move that potion into the first empty hotbar slot, or the offhand if the hotbar is full; `Loophole-Essentials` now registers twelve active addon modules under `Loophole's Essentials`: `Auto-Clutch`, `Auto-Drain`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Fast EXP`, `Jump-Reset`, `Key-Pearl`, `Pot-Refill`, and `Trigger-Bot`, and the addon builds successfully with the new `Pot-Refill` module included.
- `Auto-Pot` and `Pot-Refill` now use shorter module header descriptions so the main module text no longer redundantly repeats the detailed setting explanations shown underneath.
- `Pot-Refill` now only takes hovered matching splash potions from the main inventory grid: hotbar and offhand slots are excluded as refill sources, while empty hotbar slots and the offhand still remain valid refill destinations.
- The addon `MouseButtonSetting` loader no longer queries GLFW key-name/scancode APIs while config values are being restored before GLFW initialization: keyboard activation binds such as `Auto-Pot`'s `activation-button` now normalize through a startup-safe path, preventing the first-launch GLFW crash and allowing saved activation-button values to persist and reload correctly.
- `Shield-Breaker` is now implemented as a left-click shield punish module: while playing, not locally blocking, not eating/drinking, holding a configurable `allowed-held-items` match, and aiming at a valid player target from the same `mc.hitResult` player-targeting path that `Trigger-Bot` uses, it checks archived-style strict shield blocking plus predicted recent-block state and the archived horizontal shield-angle prediction (`shield-angle-threshold`, `shield-angle-prediction`, `shield-blocking-prediction`), swaps to a hotbar axe when the target is shield-facing within the configured angle, optionally queues one extra `shield-stun` left-click through Minecraft's normal attack click counter after a randomized dual-value `shield-stun-delay` from `0.000` to `0.050`, then optionally switches back after a randomized dual-value `switch-back-delay` from `0.000` to `0.050`, cancelling that restore if the user manually changes hotbar slots first.
- `Trigger-Bot` now hands blocking shield targets off to `Shield-Breaker` before queuing its own attack click, and its `S-Tap`/`W-Tap` sprint reset can now be packaged as a deferred tap so Shield-Breaker finishes the queued shield-stun click first and then starts the same tap timing afterward instead of overlapping the axe-break sequence.
- `Loophole-Essentials` now registers thirteen active addon modules under `Loophole's Essentials`: `Auto-Clutch`, `Auto-Drain`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Fast EXP`, `Jump-Reset`, `Key-Pearl`, `Pot-Refill`, `Shield-Breaker`, and `Trigger-Bot`.
- `Loophole-Essentials` builds successfully with the new `Shield-Breaker` module, its Trigger-Bot handoff, and the updated module registration.
- `Shield-Breaker`'s `Trigger-Bot` path now preserves the original axe swing immediately on handoff and only uses `shield-stun` for the extra delayed follow-up click afterward, so Trigger-Bot-triggered shield breaks no longer lose the opening hit; `switch-back-delay` now also supports a wider `0.000` to `0.100` range while keeping manual slot-change cancellation intact, and `Loophole-Essentials` builds successfully with the follow-up fix applied.
- `Anchor-Macro` has now been rebuilt again from scratch in `Loophole-Essentials` without reusing the archived module logic: it uses a verified tick-driven stage sequence instead of blindly chaining delayed actions, so each randomized wait only advances after the prior action has actually registered on the live respawn-anchor block state.
- `Anchor-Macro` now supports `safe-anchor-mode` with child-setting visibility: in safe mode it waits a randomized dual-value `safe-charge-delay` from `0.000` to `0.250`, swaps to hotbar glowstone, and only queues the charge click while the crosshair is still on that same freshly placed uncharged anchor; in unsafe mode it instead uses `unsafe-charge-delay` plus `unsafe-detonation-delay`, verifies that the placed anchor actually became charged first, then switches to the hotbar totem slot or the last remembered hotbar slot that held a totem and queues the detonation click only while the same charged anchor is still targeted.
- `Anchor-Macro` also includes the two universal helper flows from the requested spec: when glowstone is accidentally placed on the ground instead of into an anchor it waits a randomized `ground-glowstone-swap-delay` from `0.000` to `0.250` and swaps back to the preferred totem slot, and when the player right-clicks a respawn anchor directly it waits a randomized `anchor-target-swap-delay` from `0.000` to `0.250` before swapping to glowstone for uncharged anchors or the preferred totem slot for charged anchors.
- `Loophole-Essentials` now registers fourteen active addon modules under `Loophole's Essentials`: `Anchor-Macro`, `Auto-Clutch`, `Auto-Drain`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Fast EXP`, `Jump-Reset`, `Key-Pearl`, `Pot-Refill`, `Shield-Breaker`, and `Trigger-Bot`.
- `Loophole-Essentials` builds successfully with the new `Anchor-Macro` module and updated module registration.
- `Anchor-Macro` now performs its anchor actions as switch-and-click operations wherever possible instead of switch first and click on a later tick: delayed safe charges, unsafe detonations, and delayed `anchor-target-swap-delay` helper reactions now validate the live anchor state, switch to glowstone or the preferred totem slot, and queue the right-click in the same action pass so charging and detonating feel more immediate while still preserving the verified state checks.
- `Anchor-Macro`'s `anchor-target-swap-delay` helper no longer listens after a use packet has already been sent: it now intercepts the original right-click input on respawn anchors when the wrong item is held, cancels that original click, waits the configured delay, and then performs the replacement switch-and-click itself so the helper no longer causes an unintended second click on anchors.
- `Anchor-Macro` now treats `anchor-target-swap-delay` as the requested exception to the module's stricter verified wait-chain behavior: safe/unsafe macro waits still only advance after the previous anchor action has actually registered, but the `anchor-target-swap-delay` helper now bypasses the delayed scheduler entirely when its randomized delay resolves to `0`, immediately replacing the cancelled right-click with the same-pass switch-and-click instead of taking an extra delayed path.
- `Anchor-Macro` now preserves normal safe-mode behavior after a successful charge instead of auto-swapping to totem: duplicate-load protection is applied only when the player actually right-clicks the same already-charged anchor during the still-active macro sequence, in which case that charged-anchor click is cancelled and replaced with an immediate totem swap-and-detonate attempt in both safe and unsafe mode, preventing the accidental second glowstone load without forcing an early slot change.
- `Anchor-Macro`'s delayed charge stage now pre-selects hotbar glowstone as soon as the charge delay resolves even if the crosshair flicks away from the placed anchor, then waits to queue the right-click until the same uncharged anchor is retargeted, so brief aim flicks no longer lose the pending charge intent while still keeping the actual click gated by live anchor focus.
- `Auto-Drain` now requires true line of sight to the pickup target: its fluid-aware crosshair scan is capped by the first solid block hit from a real world clip, so visible source water under the crosshair still qualifies but water hidden behind walls or other occluding blocks no longer triggers the drain sequence; `Loophole-Essentials` builds successfully with the line-of-sight restriction applied.
- `Auto-Drain`'s occlusion clip now uses block outlines instead of only collider shapes, so cobwebs and similar interaction-blocking blocks also stop the water-target scan and the module no longer treats water behind them as clickable; `Loophole-Essentials` still builds successfully after the line-of-sight refinement.
- `Key-Bucket` is now implemented as a dual-bind bucket helper driven by two shared addon activation-input settings defaulting to `X` for lava and `Z` for water: each press swaps to a hotbar `lava_bucket` or `water_bucket` to place the matching fluid when the crosshair is not on a real pickupable water/lava source, otherwise swaps to a hotbar empty `bucket` to pick up only the first crosshair-intersected source-grade water/lava block so flowing or falling fluids do not false-trigger, and after a successful remembered pickup it can optionally wait a randomized `switch-back-delay` from `0.000` to `0.100` before restoring the original pre-placement slot unless a manual slot change cancels that child-controlled switch-back.
- `Loophole-Essentials` now registers fifteen active addon modules under `Loophole's Essentials`: `Anchor-Macro`, `Auto-Clutch`, `Auto-Drain`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Fast EXP`, `Jump-Reset`, `Key-Bucket`, `Key-Pearl`, `Pot-Refill`, `Shield-Breaker`, and `Trigger-Bot`, and the addon builds successfully with the new `Key-Bucket` module included.
- `Key-Bucket`'s pickup targeting now uses a source-only world clip instead of the earlier manual first-fluid step scan, so pressing the lava or water bind again can correctly reacquire a real pickupable source under the crosshair even when nearby flowing fluid would have made the old detection reject the pickup; `Loophole-Essentials` still builds successfully after the targeting fix.
- `Key-Bucket` now also honors a strict `targeting_block` gate before doing anything: both its place path and its source-pickup path only react while Minecraft is currently returning a real block hit result under the crosshair, and `Loophole-Essentials` still builds successfully with that restriction enabled.
- `Key-Bucket`'s `targeting_block` restriction is now scoped only to placement after follow-up testing showed fluid targeting does not always count as a normal targeted block: pickup still uses the source-only fluid-target workaround first so water/lava can be reclaimed reliably, while the fallback place path still requires a real block hit result; `Loophole-Essentials` builds successfully with the split gating fix.
- `Shield-Breaker` now includes a configurable `chance` slider from `0` to `100` percent with a default of `100`: after all normal shield-break validation passes and a hotbar axe swap is possible, both manual left-click activation and `Trigger-Bot` handoffs now roll that same chance gate before starting the swap-and-stun sequence, and `Loophole-Essentials` builds successfully with the new probability control added.
- `Elytra-Switcher` is now implemented as a fixed `Space` midair helper: when the player is airborne, playing, and has both a hotbar `elytra` and hotbar `firework_rocket`, it selects the Elytra, queues a normal Minecraft use-key click to equip it, then waits a randomized addon dual-value `firework-delay` from `0.000` to `0.100` before switching to the hotbar firework and queuing one normal right-click boost only if the player is now actually fall-flying with an Elytra equipped and no recent module-predicted rocket boost is still active.
- `Loophole-Essentials` now registers sixteen active addon modules under `Loophole's Essentials`: `Anchor-Macro`, `Auto-Clutch`, `Auto-Drain`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Elytra-Switcher`, `Fast EXP`, `Jump-Reset`, `Key-Bucket`, `Key-Pearl`, `Pot-Refill`, `Shield-Breaker`, and `Trigger-Bot`, and the addon builds successfully with the new `Elytra-Switcher` module included.
- `Elytra-Switcher`'s source identity is now fully aligned with the module rename as well: the addon class/file has been renamed from `ElytraSwitchModule.java` / `ElytraSwitchModule` to `ElytraSwitcherModule.java` / `ElytraSwitcherModule`, `LoopholeEssentialsAddon` now imports and registers the renamed class, stale addon references to the old class name have been removed, and `Loophole-Essentials` builds successfully after the rename cleanup.
- `Elytra-Switcher` now handles very short `firework-delay` values more reliably: instead of giving up after one early delayed rocket check, it keeps the post-delay firework attempt alive for a short `100ms` tick-polled retry window so sub-`0.030` timings can still wait for the Elytra equip and live fall-flying state to register before switching to the hotbar rocket and boosting without lingering as long as the earlier wider retry window; `Loophole-Essentials` builds successfully with the tightened low-delay retry fix applied.
- `Elytra-Switcher` now includes a parent `switch-back` toggle plus child `switch-back-delay` and `cancel-switch-back-on-manual-slot-change` settings following the addon's normal child-visibility pattern and hidden-setting persistence rules: after the rocket click is queued, switch-back now only restores the original selected hotbar slot after the configurable `0.000` to `0.100` delay, and a manual hotbar key or scroll change before that restore becomes ready can cancel the automatic slot return when the child option is enabled; the earlier accidental chest-armor re-equip path has been removed entirely, and `Loophole-Essentials` builds successfully with the corrected slot-only switch-back behavior.
- `Elytra-Switcher` switch-back now restores to the Elytra's original hotbar slot instead of the pre-press selected slot: the session tracks the Elytra source slot from activation, the delayed restore selects that slot after the rocket use, and the switch-back setting descriptions now match the updated behavior.
- `Elytra-Switcher` now arms its switch-back timer only after the firework step has been queued and a post-firework tick has passed, so extreme `firework-delay` and `switch-back-delay` values no longer race each other and the delayed restore consistently waits until after the rocket phase before becoming eligible.
- `Elytra-Switcher`'s switch-back timing is now enforced by session tick state instead of a millisecond scheduler callback: after the rocket click is queued it records the current client tick, refuses to arm the randomized `switch-back-delay` until a later real tick, and then starts the restore countdown from there so randomized delay combinations no longer collapse into the same scheduler window.
- `Elytra-Switcher` switch-back regression is now corrected: the broken tick-arming path that could leave switch-back unarmed has been removed, and after the rocket click is queued the module now immediately starts the restore countdown with a guaranteed minimum `50ms` post-rocket buffer while also extending the session lifetime so the Elytra slot reliably restores instead of timing out early.
- `Loophole-Essentials` currently builds successfully after the latest `Elytra-Switcher` switch-back fixes: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Anchor-Macro` unsafe timing now starts `unsafe-detonation-delay` at the moment the glowstone charge click is queued instead of only after the client later confirms the anchor charge, so `0.000` behaves as the fastest possible flawless unsafe chain and nonzero values add only the configured extra delay while detonation still waits for a real charged-anchor state; `Loophole-Essentials` currently builds successfully after the unsafe speedup adjustment with only the existing Gradle native-access and deprecation warnings reported.
- `Anchor-Macro` unsafe flow no longer burns an extra confirmation handoff tick after the glowstone click: unsafe mode now jumps straight into detonation-wait retries immediately after queueing the charge, so the totem detonation can fire on the first eligible tick where both the configured delay and real charged-anchor state are satisfied; `Loophole-Essentials` currently still builds successfully after the follow-up speedup with only the existing Gradle native-access and deprecation warnings reported.
- `Anchor-Macro` now also chains unsafe manual anchor uses instead of only freshly placed anchors: when `safe-anchor-mode` is off and the player right-clicks an uncharged respawn anchor, the module now either starts a full helper-driven unsafe charge-then-detonate sequence if glowstone is not already held or arms an immediate unsafe detonation follow-up if the player is already holding glowstone, so direct anchor clicks can now auto-explode too; `Loophole-Essentials` currently still builds successfully after the manual unsafe-chain update with only the existing Gradle native-access and deprecation warnings reported.
- `Anchor-Macro` now lets manual hotbar takeovers cancel a remembered missed-charge retry without removing the feature itself: once the pending charge stage has auto-selected glowstone and is waiting for the player to look back at the same uncharged anchor, any manual slot change away from glowstone now clears that active sequence instead of forcing the hand back, while untouched retries still keep their existing retarget-and-charge behavior; `Loophole-Essentials` currently still builds successfully after the slot-cancel refinement with only the existing Gradle native-access and deprecation warnings reported.
- The supplied `C:\Users\User\Desktop\Loophole-Essentials-AutoCartBuckUp-0.1.jar` has now been reverse engineered and its recovered `Auto-Cart` source has been archived to `Back Ups/AutoCartModule.java`; the backup JAR also shows `Auto-Cart` was previously registered as an additional historical addon module alongside the currently tracked live set, but this task only archived the recovered source and did not reintroduce it into the live `Loophole-Essentials` project.
- `Auto-Hit-Crystal` is now implemented as a shared-bind crystal helper driven by the addon activation-input setting defaulted to `X`: once per hold, while targeting a real non-air block that is not `obsidian`, `bedrock`, or any `respawn_anchor`, it waits a randomized addon dual-value `action-delay` from `0.000` to `0.100` with a default of `0.000-0.010`, swaps to a hotbar `obsidian`, and queues one normal Minecraft right-click; while the same bind stays held and both hotbar `obsidian` plus `end_crystal` remain available, it then continuously rerolls that same delay to swap to a hotbar `end_crystal` and right-click only on targeted `obsidian` or `bedrock`, and an optional `cw` toggle can instead keep swapping to crystals and queue normal left-clicks when directly targeting an `end_crystal` entity without a block target.
- `Loophole-Essentials` now registers seventeen active addon modules under `Loophole's Essentials`: `Anchor-Macro`, `Auto-Clutch`, `Auto-Drain`, `Auto-Hit-Crystal`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Elytra-Switcher`, `Fast EXP`, `Jump-Reset`, `Key-Bucket`, `Key-Pearl`, `Pot-Refill`, `Shield-Breaker`, and `Trigger-Bot`, and `Loophole-Essentials` currently builds successfully after adding `Auto-Hit-Crystal`, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s bind now defaults to `NONE` instead of `X`, and its optional `cw` crystal-hit branch now has its own separate randomized dual-value `cw-delay` setting from `0.000` to `0.100` with a default of `0.000-0.010` instead of sharing the module's main `action-delay`; `Loophole-Essentials` currently still builds successfully after the timing/default-bind adjustment, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s obsidian pre-tick branch now keeps rechecking continuously while the activation bind remains held and a valid non-`obsidian`/non-`bedrock`/non-`respawn_anchor` block is targeted, but it only consumes that hold after the module actually swaps to hotbar `obsidian` and queues the normal Minecraft right-click; failed or interrupted delayed attempts no longer force a release before retrying, while a real obsidian place still requires physically releasing and pressing the bind again for the next obsidian cycle. `Loophole-Essentials` currently still builds successfully after the hold-until-place behavior correction, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal` now separates its placement timing controls as requested: `obsidian-delay` now controls the randomized `0.000` to `0.100` delay before the held non-crystal-target obsidian place attempt, `crystal-delay` now independently controls the randomized `0.000` to `0.100` delay before each continuous crystal place on targeted `obsidian` or `bedrock`, and the optional `cw` branch still keeps its own separate `cw-delay`; `Loophole-Essentials` currently still builds successfully after the placement-delay split, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal` zero-delay responsiveness is now corrected: pressing the activation bind now immediately attempts the current obsidian/crystal/CW branch instead of waiting for the next tick, and any randomized delay that resolves to `0.000` now bypasses the delayed scheduler for that first action pass so the module no longer feels artificially slowed by an extra scheduler/tick handoff; `Loophole-Essentials` currently builds successfully after the responsiveness fix, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal` now also suppresses the next tick's action pass after an immediate press-triggered module click so the bind-event path cannot immediately stack a second block/entity action on the following tick-start pass; this removes the module's same-cycle double-action pattern that could trip Grim's `MultiActionsF` block/entity overlap check while keeping the direct-press responsiveness fix intact, and `Loophole-Essentials` currently still builds successfully after the Grim-compatibility adjustment, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s hold lock is now narrowed correctly: crystal placement and CW crystal breaking continue to run normally while the bind stays held, but once either of those crystal actions is queued the module locks only the obsidian-placement branch for the rest of that physical hold, so new obsidian will not place again until the key is physically released and pressed again; `Loophole-Essentials` currently still builds successfully after the corrected hold-lock refinement, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal` now only treats obsidian as "used this hold" after the placement visibly registers in the world: queued obsidian clicks now arm a short pending confirmation window tied to the expected placed block position, failed placements blocked by entities no longer consume or lock the obsidian branch, and the later crystal-action obsidian lock now only arms if that earlier obsidian placement actually became a real `obsidian` block before the physical release/re-press requirement applies; `Loophole-Essentials` currently still builds successfully after the placement-confirmation refinement, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s confirmed-placement lock regression is now fixed too: if a crystal place or CW crystal break happens while the earlier obsidian place is still inside that short pending confirmation window, the module now remembers that lock request and promotes it into the real obsidian-placement lock as soon as the world actually confirms the obsidian block, so successful obsidian->crystal sequences still block further obsidian until physical release/re-press while entity-blocked failed placements still do not falsely lock; `Loophole-Essentials` currently still builds successfully after the pending-lock handoff fix, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal` now keeps that pending obsidian confirmation alive longer in multiplayer too: the expected obsidian placement window was widened from `3` ticks to `10` ticks so the later crystal-action lock no longer gets dropped just because the world/state acknowledgment arrived a few ticks late, while failed entity-blocked placements still avoid false obsidian-consume/lock behavior; `Loophole-Essentials` currently still builds successfully after the widened confirmation-window adjustment, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s obsidian lock is now simplified again as requested: crystal placement and CW crystal breaking now immediately latch the obsidian-placement branch off and keep it off until the trigger is physically released, instead of waiting for any later pending-lock promotion path, while the separate obsidian-consumed confirmation still remains in place so failed entity-blocked obsidian clicks do not falsely count as a successful obsidian placement; `Loophole-Essentials` currently still builds successfully after the release-only lock simplification, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s mixed-delay hotbar bounce and failed-server-place recovery are now corrected: after a crystal place or CW hit click is queued, the module enters a short pending-confirmation window that pauses new obsidian/crystal actions until the expected crystal entity either really appears or the targeted crystal really disappears, instead of immediately hard-locking obsidian for the rest of the hold; crystal placement also now refuses to requeue while a crystal is already occupying the targeted base, so delayed server acknowledgment no longer causes repeated hotbar yank/bounce behavior or leaves the module stranded unable to crystal after a failed place attempt. `Loophole-Essentials` currently builds successfully after the `Auto-Hit-Crystal` recovery fix, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s tiny positive delays now use a frame-polled ready-timestamp path instead of `runDelayedTask()` wakeups: `0.000` still keeps its true immediate behavior, but values like `0.001+` now arm a millisecond target that is checked during render/tick processing so they no longer collapse into an oversized scheduler/tick delay; the earlier bounce/recovery confirmation guard remains intact, and `Loophole-Essentials` currently still builds successfully after the timing-granularity fix with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s CW break cadence is now separated from the crystal-placement confirmation guard: the optional crystal-hit branch no longer pauses after every queued attack waiting for the targeted crystal entity to disappear, so `cw-delay` now directly controls repeated break attempts much more closely while the placement-side bounce/failure recovery confirmation window still stays active for crystal placement. `Loophole-Essentials` currently still builds successfully after the CW break-delay fix, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s CW break timing now also uses a more direct delayed callback path instead of relying on the render-polled ready-timestamp loop used for placement-side safety: tiny positive `cw-delay` values should now land closer to `0`-delay feel because repeated crystal-hit attempts are no longer waiting for the next render/tick poll just to wake up, while the safer frame-polled confirmation path still remains in place for obsidian/crystal placement flow. `Loophole-Essentials` currently still builds successfully after the CW low-delay responsiveness refinement, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Hit-Crystal`'s blocked-obsidian retry stall is now corrected: when a held obsidian place attempt is still inside its multiplayer confirmation window but the player has already moved to a different target, the module now immediately clears that stale pending state if the old placement spot is obviously still occupied by a block or blocking entity, so looking from an entity-blocked invalid spot to a valid neighboring spot no longer forces waiting out the full confirmation timer before the next obsidian place can fire. `Loophole-Essentials` currently builds successfully after the stale pending-obsidian retry fix, with only the existing Gradle native-access and deprecation warnings reported.
- `Inventory-Totem` is now implemented as an inventory-screen totem helper: while only the player inventory screen is open, it keeps a totem in the offhand when available, remembers the most recent totem hotbar slot across the session, refills that remembered hotbar slot while the offhand still holds a totem, optionally smooth-drags the GUI cursor toward the chosen totem with a child `smooth-cursor` section (`cursor-move-duration`, `cursor-curve-mode`, and hidden persistent `custom-curve-chance`), uses a randomized dual-value `swap-delay` from `0.000` to `0.100` before sending the same vanilla-style offhand or hotbar swap action, and also listens for the user's current vanilla inventory key so pressing it in normal play reselects the remembered totem hotbar slot before the inventory opens. `Loophole-Essentials` now registers eighteen active addon modules under `Loophole's Essentials`: `Anchor-Macro`, `Auto-Clutch`, `Auto-Drain`, `Auto-Hit-Crystal`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Elytra-Switcher`, `Fast EXP`, `Inventory-Totem`, `Jump-Reset`, `Key-Bucket`, `Key-Pearl`, `Pot-Refill`, `Shield-Breaker`, and `Trigger-Bot`, and `Loophole-Essentials` currently builds successfully after adding `Inventory-Totem`, with only the existing Gradle native-access and deprecation warnings reported.
- `Inventory-Totem`'s cursor targeting has now been refined: smooth cursor motion is advanced during inventory rendering instead of only on pre-tick so it no longer visually teleports in coarse tick-sized jumps, a new `randomize-hover-point` option can aim at a random in-slot point instead of always the slot center, and source selection now always chooses the totem slot closest to the current cursor position instead of preferring the remembered hotbar totem first. `Loophole-Essentials` currently still builds successfully after the `Inventory-Totem` cursor-selection refinement, with only the existing Gradle native-access and deprecation warnings reported.
- `Inventory-Totem`'s randomized-hover and swap-confirmation flow is now corrected: once the cursor first enters the chosen totem slot the module now locks that hover position instead of continuing to drift around inside the slot until `swap-delay` finishes, and after sending the offhand or remembered-hotbar swap it now waits through a short confirmation window for the target slot to actually contain a totem before clearing the active session, preventing premature retargeting or bouncing between nearby totems when the equip has not visibly landed yet. `Loophole-Essentials` currently still builds successfully after the `Inventory-Totem` confirmation-lock fix, with only the existing Gradle native-access and deprecation warnings reported.
- `AimAssist` has now been rebuilt from scratch in `Loophole-Essentials` as a controller-style assist module instead of a lock-on tracker: it smoothly interpolates only a fraction of the remaining yaw/pitch error each render frame, supports player/mob targeting, configurable FOV and range, click gating, exact item-ID allowlist or blacklist filtering, selectable aim-point hitbox anchors with dynamic and randomized offsets, axis-specific strength and smoothing with optional shared fallbacks, linear/dynamic/exponential acceleration curves, configurable prediction with optional ping compensation, target stickiness, and humanized variable-speed micro-biasing so it helps acquire targets without snapping or perfectly tracking them.
- `AimAssist` now also includes the requested anti-aimlock stop system with independent stop-point selection: `Angle`, `Screen Radius`, and `Hitbox Percentage` stop modes scale through optional distance-aware dynamic radius logic, and once the crosshair enters that stop zone the module either fully backs off or smoothly fades assistance toward zero based on `fade-near-target` and `fade-strength`, so the player keeps final micro-adjustment control instead of the module forcing center hold; `Loophole-Essentials` now registers nineteen active addon modules under `Loophole's Essentials`: `AimAssist`, `Anchor-Macro`, `Auto-Clutch`, `Auto-Drain`, `Auto-Hit-Crystal`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Elytra-Switcher`, `Fast EXP`, `Inventory-Totem`, `Jump-Reset`, `Key-Bucket`, `Key-Pearl`, `Pot-Refill`, `Shield-Breaker`, and `Trigger-Bot`, and `Loophole-Essentials` currently builds successfully after adding the rebuilt `AimAssist`, with only the existing Gradle native-access and deprecation warnings reported.
- `AimAssist`'s held-item gating has now been simplified to match `Trigger-Bot`-style fragment filtering instead of exact Minecraft item IDs: the item-filter text setting is now `allowed-held-items`, it matches main-hand `descriptionId` fragments such as `sword,_axe,mace`, those fragments are now the default values, and a new `stop-while-breaking-blocks` toggle now suspends AimAssist during active block mining so left-click breaking keeps full manual control; `Loophole-Essentials` currently still builds successfully after the `AimAssist` filter/mining-safety update, with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot` has now been archived from the live `Loophole-Essentials` source tree again: the current `TriggerBotModule.java` source was copied into `Back Ups/TriggerBotModule.java`, removed from active addon registration, and deleted from `Loophole-Essentials/src/main/java`.
- `Shield-Breaker` has been decoupled from the archived `Trigger-Bot`: its live source no longer depends on `TriggerBotModule` handoff types or deferred tap callbacks, so the manual shield-break flow still compiles and runs cleanly without the archived module present.
- `Loophole-Essentials` now registers eighteen active addon modules under `Loophole's Essentials`: `AimAssist`, `Anchor-Macro`, `Auto-Clutch`, `Auto-Drain`, `Auto-Hit-Crystal`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Elytra-Switcher`, `Fast EXP`, `Inventory-Totem`, `Jump-Reset`, `Key-Bucket`, `Key-Pearl`, `Pot-Refill`, and `Shield-Breaker`, and `.\gradlew.bat build` currently completes successfully with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot` has now been rebuilt again from scratch in the live `Loophole-Essentials` source tree without reusing the archived module source: the new implementation uses render-frame-first candidate creation plus immediate final-frame revalidation before every click so flick-away target loss cancels the swing instead of wasting air attacks.
- The rebuilt `Trigger-Bot` now includes the requested configurable `allowed-held-items` filter defaulting to `#sword,#_axe,mace`, randomized dual-value `attack-progress`, grounded/critical/uppercut branch validation, optional `W-Tap` / `S-Tap` sprint resets with safe movement restoration, an optional hard-disabled `attack-delay` path that continuously revalidates during the delay window, and optional humanization misses for knockback, out-of-range, and cobweb scenarios that always abort if the intended player becomes a valid Trigger-Bot target again.
- `Loophole-Essentials` now registers nineteen active addon modules under `Loophole's Essentials`: `AimAssist`, `Anchor-Macro`, `Auto-Clutch`, `Auto-Drain`, `Auto-Hit-Crystal`, `Auto-Lunge`, `Auto-Pot`, `Auto-Sprint`, `Auto-Web`, `Block-Spam`, `Elytra-Switcher`, `Fast EXP`, `Inventory-Totem`, `Jump-Reset`, `Key-Bucket`, `Key-Pearl`, `Pot-Refill`, `Shield-Breaker`, and `Trigger-Bot`, and `.\gradlew.bat build` currently completes successfully again with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot`'s live target acquisition has now been reworked to reuse the archived module's stable-target system instead of the newer render-created real-attack candidate path: render frames now rebuild a same-player stability counter, tick-start real-attack candidate creation now requires that archived-style stable crosshair lock before queuing a hit, and live target/branch revalidation now flows through the shared archived-style `AttackContext` helper while leaving the newer attack-delay, sprint-reset, and humanization systems intact.
- `Loophole-Essentials` currently still builds successfully after the `Trigger-Bot` targeting-system transplant from `Back Ups/TriggerBotModule.java`: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot`'s first-hit responsiveness is now frame-driven again when a valid player first enters the crosshair: the stable-target warmup was reduced to a single render frame and candidate creation now also runs during `RenderWorldEvent`, so with `attack-delay` disabled the module can arm and queue the opening hit on the first valid render frame instead of waiting for the next tick-start cycle.
- `Trigger-Bot` still keeps its immediate pre-click revalidation and normal cooldown gating after the first-hit responsiveness fix, and `Loophole-Essentials` currently builds successfully after the update: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- The later archived comparison showed that the warmup frames were not the real cause of the first-hit stall: the archived `Trigger-Bot` also used `2` stable-target frames, but the live rebuild had added an extra `mc.player.distanceTo(target) > getAttackRange()` rejection after a valid player crosshair hit was already found, which could delay the opening swing until the player was much closer than the actual entity hitbox reach allowed.
- `Trigger-Bot` has now been corrected by restoring the original `2` warmup-frame behavior and removing that live-only extra center-distance range gate from `findCurrentAttackContext()`, so first-hit activation now follows the archived crosshair-hit behavior again while keeping the newer pending-action, revalidation, sprint-reset, and humanization systems intact; `Loophole-Essentials` currently still builds successfully after the fix with only the existing Gradle native-access and deprecation warnings reported.
- After confirming the range-gate fix solved the real first-hit stall, `Trigger-Bot`'s stable-target warmup has now been reduced again from `2` frames to `1` frame so the module can commit sooner while keeping the corrected archived-style crosshair-target flow.
- `Loophole-Essentials` currently still builds successfully after the `1`-frame warmup adjustment: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot`'s stable-target warmup has now been removed entirely instead of being set to a cosmetic `0`: the real-attack candidate path no longer waits on any archived stable-target frame counter before arming, and the now-unused stable-target bookkeeping was removed from the live module so opening hits can queue as soon as the current crosshair target and combat-state validation pass.
- `Loophole-Essentials` currently still builds successfully after removing the `Trigger-Bot` warmup gate entirely: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot`'s sprint-reset taps now honor the configured `tap-duration` again instead of collapsing to one tick: tap continuation no longer reuses the start-only validation gate that rejected already-active taps, so started `W-Tap` and `S-Tap` sessions now keep running for their full configured duration unless the real safety abort conditions break them.
- `Trigger-Bot`'s humanization misses now follow the attack-progress cooldown gate consistently: knockback, out-of-range, and cobweb misses each cache a randomized attack-progress threshold when queued and then wait to fire until that cooldown requirement is met while still aborting immediately if the intended player becomes a valid real Trigger-Bot target again.
- `Trigger-Bot`'s delayed real-attack revalidation is now explicit about still being on the same player target when the delay resolves, `out-of-range-misses` no longer depend on vanilla in-range entity hit results and instead use an extended crosshair ray check out to the configured window, the `out-of-range-distance-window` slider now has a minimum of `3.000`, and `cobweb-misses` now require the tracked player behind the cobweb to be within true entity attack reach instead of only block-target reach.
- `Loophole-Essentials` currently still builds successfully after the latest `Trigger-Bot` tap, cooldown-gating, out-of-range, and cobweb fixes: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot`'s knockback misses now stay alive through temporary valid-hit windows instead of being discarded as soon as the attacker becomes hittable again, and `out-of-range-misses` now respect the module's sprint requirement, roll their chance once per entry into the configured distance window, and consume at most one miss per window entry; the out-of-range window now also defaults to `3.000-4.000` so the lower handle starts at `3.0` instead of above it.
- `Loophole-Essentials` currently still builds successfully after the latest `Trigger-Bot` knockback/out-of-range refinements: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot` now keeps `out-of-range-misses` strictly grounded on true `onGround()` state, restores the default `out-of-range-distance-window` to `3.500-4.000` while keeping the slider bounded to `3.000-6.000`, adds a new `knockback-miss-distance-window` slider plus a `knockback-miss-reduce-back-to-back-chance` toggle that halves the miss chance for each consecutive unanswered incoming hit, and resets that streak when Trigger-Bot queues a real hit back.
- `Shield-Breaker` once again accepts Trigger-Bot handoffs through a dedicated `tryStartFromTriggerBot(Player)` path: Trigger-Bot real attack clicks now count for shield breaking by letting Shield-Breaker swap to the axe and queue the opening click itself when the target is in a valid shield-break state, and `.\gradlew.bat build` currently still completes successfully after these Trigger-Bot / Shield-Breaker updates with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot` shield-break handoffs now defer grounded `S-Tap` / `W-Tap` startup into Shield-Breaker instead of starting the tap on the opening axe click: Trigger-Bot now passes a deferred tap payload during valid shield-break handoffs, and Shield-Breaker only releases that tap after the shield-stun follow-up click fires, while still falling back to immediate post-click tap startup when no shield-break handoff occurs.
- `Loophole-Essentials` currently still builds successfully after the deferred shield-break tap timing update: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Shield-Breaker`'s queued axe clicks now arm immediately in the live sequence instead of being wrapped in an extra nested `mc.execute()` hop, so Trigger-Bot shield-break handoffs now queue the opening axe click in the same path as the slot swap and should stop losing some visible axe-hit shield-break openings to delayed input timing.
- `Trigger-Bot` now exposes a `wait-for-shield-break-follow-up-click` sprint-reset option under `Sprint Reset`, defaulted on: grounded Trigger-Bot hits still create the same deferred tap payload, but handoffs now only wait for Shield-Breaker's second shield-stun click when that option is enabled; disabling it restores immediate post-opening-click `S-Tap` / `W-Tap` startup even during shield-break handoffs. `Loophole-Essentials` currently still builds successfully after these updates via `.\gradlew.bat build`, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Mace` is now implemented as a new combat swap module in `Loophole-Essentials`: it adds `Automatic` and `Manual` trigger modes, configurable `allowed-held-items`, a downward `smash-velocity-threshold`, optional ground-proximity gating for breach logic, a density-mace `Smash Attack` branch, axe-prep plus density follow-up `Stun Slam` logic with a `stun-prep-distance-window`, breach-mace crit/grounded `Breach Swap` logic with a configurable grounded chance split versus normal hits, and an axe-only `Shield Break` branch with optional second click and per-branch randomized switch-back delays.
- `Auto-Mace` now follows the addon's normal child-setting visibility and persistence patterns, is registered in `LoopholeEssentialsAddon` as a live addon module under `Loophole's Essentials`, and `Shield-Breaker` now ignores already-cancelled left-click events so `Auto-Mace` `Manual` mode can safely take over the click path without overlapping the shield-break listener.
- `Loophole-Essentials` currently still builds successfully after adding `Auto-Mace`: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Mace` targeting/click validation is now tightened to match `Trigger-Bot` more closely: the module already used the same `KeyMapping.click(((AccessorKeyMapping) mc.options.keyAttack).loopholeEssentials$getBoundKey())` click path, but its real problem was committing clicks immediately after detection instead of revalidating on the final execute pass.
- `Auto-Mace` now defers its opening attack click into `mc.execute()` and rechecks that the same player is still the exact live `mc.hitResult` crosshair target before every opening click and every queued follow-up click, aborting the active swap sequence and restoring the original slot instead of air-swinging if the target was only caught during a fast flick.
- `Loophole-Essentials` currently still builds successfully after the `Auto-Mace` targeting fix: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- The remaining `Auto-Mace` flick issue was traced to sequence startup rather than only the click itself: the module still switched to the combo weapon before the final live-target validation, so a stale manual/held attack state could still slip through after the hotbar swap even when the later click gate aborted.
- `Auto-Mace` now keeps the original hotbar slot selected until the exact execute-time opening-click commit, only swapping to the primary combo slot after the same-player live crosshair revalidation passes on that final opening-click path; `Loophole-Essentials` currently still builds successfully after this sequence-start fix via `.\gradlew.bat build`, with only the existing Gradle native-access and deprecation warnings reported.
- The remaining mismatch with `Trigger-Bot` was a separate `Auto-Mace` branch, not the shared `KeyMapping.click(...)` path itself: when grounded sprint breach logic failed its chance roll, `Auto-Mace` returned an automatic `NORMAL_HIT` intent that still called `KeyMapping.click(...)` directly without the final execute-time same-target revalidation that `Trigger-Bot` uses.
- `Auto-Mace` automatic normal-hit fallback now also captures the target UUID and defers its unchanged `KeyMapping.click(...)` call into an execute-time revalidation pass that requires the same live direct crosshair player, in-range distance, and full cooldown before clicking; `Loophole-Essentials` currently still builds successfully after this remaining normal-hit fix via `.\gradlew.bat build`, with only the existing Gradle native-access and deprecation warnings reported.
- Follow-up investigation suggests the common remaining problem path is the slot swap itself rather than the shared `KeyMapping.click(...)` helper: `Auto-Mace` was still selecting the mace/axe slot and queueing the guarded click in the same `mc.execute()` pass, so if the live crosshair/attack state destabilized when the slot changed, every swap branch could still air-swing even though the pre-swap validation passed.
- `Auto-Mace` opening and follow-up sequence clicks now split slot changes and attack clicks into separate execute-time passes: after switching to the primary or secondary hotbar slot, the module immediately re-enters `mc.execute()`, revalidates the same-player live crosshair target again with the new slot already selected, and only then calls the unchanged `KeyMapping.click(...)`; `Loophole-Essentials` currently still builds successfully after this post-swap revalidation change via `.\gradlew.bat build`, with only the existing Gradle native-access and deprecation warnings reported.
- Reverted the pinned `Auto-Mace` execute-time targeting rollback fixes from `AGENTS.md:L153-L161`: automatic normal hits no longer defer through same-target execute-time revalidation, opening and follow-up sequence clicks now switch slots and call the unchanged `KeyMapping.click(...)` in the same path again, and the module is back on its earlier pre-fix swap/click flow.
- `Loophole-Essentials` currently still builds successfully after reverting those pinned `Auto-Mace` changes: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- Reverted the latest `Auto-Mace` implementation attempt as well because it did not solve the problem: `AutoMaceModule` no longer uses the local `PendingAutomaticTrigger` automatic-mode wrapper or the `PendingClickCommit` queued-hit commit guard, automatic runtime goes back to starting direct `createAttackIntent(false)` intents, and opening/follow-up sequence progression again advances immediately after queuing the unchanged `KeyMapping.click(...)` on the older swap/click flow described at `AGENTS.md:L162-L163`.
- `Loophole-Essentials` currently still builds successfully after reverting those latest `Auto-Mace` code changes: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Mace` now keeps the unchanged `KeyMapping.click(...)` attack path but closes the `mc.hitResult` decoupling window differently: a new `AccessorMinecraft` mixin can pin `Minecraft.hitResult`, queued Auto-Mace attacks are staged through a local tick-start pending wrapper so only `ClientTickStartEvent` performs the real queued attack, and the module now overwrites `mc.hitResult` with an `EntityHitResult` for the resolved live direct-crosshair target immediately before each queued click.
- `Auto-Mace` follow-up safety is also tighter under that tick-start queued-click flow: opening and follow-up sequence progression now waits until the tick-start queued click actually fires before scheduling the next stage, `RenderWorldEvent` no longer reaches the real queued attack path, and `isSequenceAttackStillValid(...)` now also requires the stored sequence target to still be a live direct-crosshair player within attack range before a follow-up click can arm.
- `Loophole-Essentials` currently still builds successfully after the latest `Auto-Mace` tick-start hitResult pinning change: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Mace` opener-delay handling is now corrected in the tick-start queued-click flow: `ClientTickStartEvent` now runs `handleRuntime()` before the single `processPendingTickClick()` consume pass, and the old re-entrant same-tick `queueAttackClick(...) -> processPendingTickClick()` duplication has been removed so newly armed automatic opening clicks can be created first and then consumed in the same tick-start pass instead of relying on the nested path.
- `Auto-Mace` pinned-target revalidation no longer adds the extra player-center distance gate after a valid direct crosshair player hit: `isPinnedAttackTargetStillValid(...)` now trusts `isValidTarget(...)` plus the live direct-crosshair match, preventing edge-of-hitbox opener clicks from being rejected just because `mc.player.distanceTo(target)` is slightly outside the nominal center-to-center range while approaching.
- `Loophole-Essentials` currently still builds successfully after the latest `Auto-Mace` opener-delay and pinned-target validation fix: running `.\gradlew.bat build` in the addon project completed with `BUILD SUCCESSFUL`, with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot` humanization settings now persist reliably when their parent sections are hidden during config loading: `knockback-misses`, `out-of-range-misses`, and `cobweb-misses` are included in the module's persistent hidden-setting collection alongside their existing child controls, preventing those nested toggles from reverting to defaults after restarting the game. `Loophole-Essentials` currently still builds successfully after the persistence fix via `.\gradlew.bat build`, with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot` now has an opt-in `opponent-angle-check` setting under `Sprint Reset` with a persistent child `opponent-angle-threshold` slider from `0.0` to `180.0` degrees, defaulting to `90.0`; the child hides while its parent is disabled, and both remain hidden while sprint reset mode is `Off`. Tap activation mirrors Shield-Breaker's horizontal opponent-facing calculation and only starts when the computed angle is less than or equal to the configured threshold, including Shield-Breaker-deferred and humanization-triggered taps. `.\gradlew.bat build` currently completes successfully with only the existing Gradle native-access and deprecation warnings reported.
- `Auto-Web`, `Block-Spam`, and `Fast EXP` now each expose an independent randomized dual-value `start-delay` before their normal repeating `use-delay` cycle begins; defaults remain `0.000-0.000`, with `Auto-Web`/`Block-Spam` capped at `0.500` and `Fast EXP` capped at `0.100`. The first automated repeat occurs after the independently randomized start and use delays, then later repeats use only `use-delay`, and pending startup/repeat actions still invalidate immediately when activation or runtime conditions break.
- `Auto-Web` and `Block-Spam` now also expose an independent persistent `immediate-first-use` toggle, defaulted on, which queues one immediate module right-click for keyboard, Middle Click, Mouse 4, or Mouse 5 activation before the delayed repeat cycle. The toggle dynamically disappears when `Right Click` is selected because vanilla already supplies that initial click, while its hidden value persists and returns when another bind is selected; `Fast EXP` remains fixed to Right Click and therefore does not display the redundant toggle. `.\gradlew.bat build` currently completes successfully after these timing/UI additions with only the existing Gradle native-access and deprecation warnings reported.
- `Trigger-Bot` attack-delay investigation confirmed that the dual range setting and ClickCrystals randomizer return a true `0` for a saved `0-0` range, but zero-delay real attacks were still entering the generic pending-action processor and `mc.execute()` finalization handoff. Real attacks now synchronously run the normal final revalidation and click commit on the candidate tick whenever attack delay is disabled or its randomized value resolves to `0`, while positive delays retain the existing frame-polled pending/revalidation path. `.\gradlew.bat build` currently completes successfully after the zero-delay bypass with only the existing Gradle native-access and deprecation warnings reported.
